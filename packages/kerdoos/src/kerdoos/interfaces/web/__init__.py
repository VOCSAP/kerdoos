"""Kerdoos WebUI interface (post-MVP, structural skeleton only at Phase 0).

Thin interface on top of the future `core/app` use-case layer (ADR 0001).
Nothing here may contain business logic (invariant #9, CLAUDE.md).
"""
