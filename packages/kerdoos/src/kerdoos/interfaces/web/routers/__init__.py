"""Empty router placeholders for the WebUI (Phase 0: structural only).

Real routers (config, state, run) land with the `core/app` use-case layer
(ADR 0001, post-MVP). No auth, no business logic here yet.
"""
