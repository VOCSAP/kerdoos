# Reviewed false positive (security-auditor confirmed, gate D, 2026-07-06).
# The flagged high-entropy token on the line below is a feature-flag identifier
# inside the "ssrFeatureFlags" array of a PUBLIC Kabum product-page fixture
# (captured Next.js state). It is a UI campaign flag name, NOT a credential.
# The literal token is intentionally not quoted here so staging this ignore file
# does not itself re-trigger the scanner.
# Surgical single-finding ignore by exact fingerprint -- NOT a path allowlist
# (tests/fixtures/ stays scanned so a real token cannot slip in later).
tests/fixtures/kabum_aw3225qf.html:generic-api-key:97

# Reviewed false positive (Terabyte SSR fixture, 2026-07-07).
# The flagged 32-char token is the PUBLIC Cloudflare Web Analytics beacon token
# in a data-cf-beacon attribute (RUM telemetry that Cloudflare injects into the
# page HTML; visible to anyone viewing source). It is NOT a credential. The
# literal token is intentionally not quoted here so staging this ignore file does
# not itself re-trigger the scanner. Surgical single-finding ignore by exact
# fingerprint -- tests/fixtures/ stays scanned.
tests/fixtures/terabyte_40561.html:generic-api-key:3825

# Reviewed false positives (Magalu resolved UC fixture, 2026-07-07).
# Three PUBLIC client-side RUM/analytics tokens embedded in the page HTML
# (visible to anyone viewing source), NOT credentials: a Datadog RUM clientToken,
# an Akamai mPulse Boomerang API key, and a RUM (spdt) config key. Literal tokens
# are intentionally not quoted here so staging this ignore file does not itself
# re-trigger the scanner. Surgical per-finding ignores by exact fingerprint;
# tests/fixtures/ stays scanned.
tests/fixtures/magalu_uc.html:generic-api-key:83
tests/fixtures/magalu_uc.html:generic-api-key:110
tests/fixtures/magalu_uc.html:generic-api-key:162

# Reviewed false positives (Magalu camoufox fixtures). The same PUBLIC
# client-side tokens as above: Datadog RUM clientToken (line 83), Akamai mPulse
# Boomerang API key and a QuestionPro survey widget key (line 110), a RUM (spdt)
# config key (line 116). Literal tokens are intentionally not quoted here.
tests/fixtures/magalu_bab5438g3h_camoufox.html:generic-api-key:83
tests/fixtures/magalu_bab5438g3h_camoufox.html:generic-api-key:110
tests/fixtures/magalu_238968700_camoufox.html:generic-api-key:83
tests/fixtures/magalu_238968700_camoufox.html:generic-api-key:110
tests/fixtures/magalu_238968700_camoufox.html:generic-api-key:116

# Carte 18bdfd9f: the same 11 reviewed false positives above, duplicated
# because packages/autolycos/tests/fixtures/ holds a byte-identical COPY of
# these 9 dumps (autolycos's own test suite needs them ahead of a future
# `git subtree split`; kerdoos keeps its originals under tests/fixtures/).
# Same fingerprints, same secrets, new path -- gitleaks fingerprints the
# path, so each copy needs its own entry.
packages/autolycos/tests/fixtures/kabum_aw3225qf.html:generic-api-key:97
packages/autolycos/tests/fixtures/terabyte_40561.html:generic-api-key:3825
packages/autolycos/tests/fixtures/magalu_uc.html:generic-api-key:83
packages/autolycos/tests/fixtures/magalu_uc.html:generic-api-key:110
packages/autolycos/tests/fixtures/magalu_uc.html:generic-api-key:162
packages/autolycos/tests/fixtures/magalu_bab5438g3h_camoufox.html:generic-api-key:83
packages/autolycos/tests/fixtures/magalu_bab5438g3h_camoufox.html:generic-api-key:110
packages/autolycos/tests/fixtures/magalu_238968700_camoufox.html:generic-api-key:83
packages/autolycos/tests/fixtures/magalu_238968700_camoufox.html:generic-api-key:110
packages/autolycos/tests/fixtures/magalu_238968700_camoufox.html:generic-api-key:116
